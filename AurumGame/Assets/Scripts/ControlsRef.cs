using UnityEngine;
using System.Collections;

public class ControlsRef : MonoBehaviour {

	// Use this for initialization
	void Start () {

		// Arrow Keys = Movement
		// A = Interact with doors and objects
		// X = hide in hideable objects
		// Left Shift = Sprints
		// Left Control = crouch/slow walk








	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
